{
  "name": "@reserve-protocol/dtf-rebalance-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Internal SDK-aware rebalance tooling for repo simulations and operations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc --project tsconfig.json"
  },
  "license": "BlueOak-1.0.0",
  "peerDependencies": {
    "@reserve-protocol/dtf-rebalance-lib": "^3.3.0"
  },
  "dependencies": {
    "@reserve-protocol/sdk": "^0.0.3"
  }
}
